pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "Vocalize AI"
include ':app'

apply from: 'capacitor.settings.gradle'

