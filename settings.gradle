pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "Vocalize AI"
include ':app'
project(':app').projectDir = new File('android/app')

apply from: 'android/capacitor.settings.gradle'

